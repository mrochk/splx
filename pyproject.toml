[project]
name = "bsplx"
version = "0.1.0"
description = "B-splines in Jax."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "beartype>=0.22.9",
    "jax>=0.10.0",
    "jaxtyping>=0.3.9",
]
